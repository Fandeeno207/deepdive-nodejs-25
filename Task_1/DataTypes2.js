
function performOperation() {
    var r = readLine();
    const PI = Math.PI
    // Print the area of the circle:
    console.log(PI*r**2)
    // Print the perimeter of the circle:
    console.log(2*PI*r)
}
